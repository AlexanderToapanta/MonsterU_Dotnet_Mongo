@{
    Layout = null;
    var usuario = ViewBag.UsuarioLogueado;
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- PrimeIcons -->
    <link href="https://cdn.jsdelivr.net/npm/primeicons/primeicons.css" rel="stylesheet" />

    <!-- Estilos -->
    <style>
        .layout-container {
            display: grid;
            grid-template-rows: 65px auto 60px;
            min-height: 100vh;
            width: 100%;
        }

        .layout-north {
            grid-row: 1;
            background-color: #f8f9fa;
            border-bottom: 1px solid #dee2e6;
            padding: 0.5rem 1rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            position: relative;
            z-index: 10;
        }

        .layout-center {
            grid-row: 2;
            padding: 1rem;
            overflow: auto;
            background-image: url('https://www.bangor.ac.uk/sites/default/files/styles/16x9_1100w/public/2020-05/CB%202018%20May%20%2837%29.jpg?h=41ef3158&itok=zGs3cG5Q');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            background-repeat: no-repeat;
            position: relative;
        }

            .layout-center::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background: rgba(0, 0, 0, 0.4);
                z-index: 0;
            }

        .layout-content {
            position: relative;
            z-index: 1;
            
        }

        .layout-south {
            grid-row: 3;
            background-color: rgba(233,236,239,0.9);
            border-top: 1px solid #dee2e6;
            padding: 0.5rem 1rem;
            display: flex;
            align-items: center;
            z-index: 10;
        }

        .app-header {
            font-size: 1.6rem;
            font-weight: bold;
            color: #6C7BFF;
        }


        .user-menu {
            display: flex;
            justify-content: center;
            padding: 10px;
        }

        .menu-form {
            display: flex;
            gap: 10px;
            align-items: center;
        }

            .menu-form .dropdown .dropdown-menu {
                position: absolute;
            }
    </style>

    @RenderSection("styles", required: false)
</head>
<body>

    <div class="layout-container">

        <!-- ==================================== HEADER ====================================== -->
        <div class="layout-north">

            <!-- TÍTULO -->
            <div class="app-header">MONSTER UNIVERSITY</div>

            <!-- MENÚS -->
            <div class="user-menu">
                <div class="menu-form d-flex align-items-center">

                    <!-- HOME -->
                    <a href="@Url.Action("Index", "Home")" class="btn btn-primary menu-btn">
                        <i class="pi pi-home"></i> Home
                    </a>

                    <!-- PERSONAL -->
                    <div class="dropdown">
                        <button class="btn btn-secondary dropdown-toggle menu-btn" type="button" data-bs-toggle="dropdown">
                            Personal
                        </button>
                        <ul class="dropdown-menu">
                            <li>
                                @Html.ActionLink("🆕 Crear Usuario", "crearusuario",
                                     "ControladorUsuario", null, new { @class = "dropdown-item" })
                            </li>
                            <li><hr class="dropdown-divider" /></li>
                        </ul>
                    </div>

                    <!-- FINANZAS -->
                    <div class="dropdown">
                        <button class="btn btn-secondary dropdown-toggle menu-btn" type="button" data-bs-toggle="dropdown">
                            Finanzas
                        </button>
                        <!-- Aquí irían los items del menú Finanzas -->
                    </div>

                    <!-- ACADÉMICO -->
                    <div class="dropdown">
                        <button class="btn btn-secondary dropdown-toggle menu-btn" type="button" data-bs-toggle="dropdown">
                            Académico
                        </button>
                        <!-- Aquí irían los items del menú Académico -->
                    </div>

                    <!-- SEGURIDAD -->
                    <div class="dropdown">
                        <button class="btn btn-secondary dropdown-toggle menu-btn" type="button" data-bs-toggle="dropdown">
                            Seguridad
                        </button>
                        <!-- Aquí irían los items del menú Seguridad -->
                    </div>

                    <!-- USUARIO LOGUEADO -->
                    @if (Session["Usuario"] != null)
                    {
                        <div class="dropdown">
                            <button class="btn btn-outline-primary dropdown-toggle menu-btn"
                                    data-bs-toggle="dropdown">
                                <i class="pi pi-user"></i> @ViewBag.Usuario
                            </button>

                            <ul class="dropdown-menu">
                                <li>
                                    @Html.ActionLink("🔑 Cambiar Contraseña", "cambiarcontrasena",
                                         "ControladorUsuario", null, new { @class = "dropdown-item" })
                                </li>
                                <li><hr class="dropdown-divider" /></li>
                                
                                <li>
                                    @using (Html.BeginForm("Logout", "Login", FormMethod.Post, new { id = "logoutForm" }))
                                    {
                                        @Html.AntiForgeryToken()
                                        <a href="javascript:document.getElementById('logoutForm').submit();"
                                           class="dropdown-item text-danger">
                                            <i class="pi pi-sign-out"></i> Cerrar sesión
                                        </a>
                                    }
                                </li>
                            </ul>
                        </div>
                    }
                    else
                    {
                        <!-- LOGIN SI NO ESTÁ LOGUEADO -->
                        @Html.ActionLink("Iniciar Sesión", "Login", "Login",
                        null, new { @class = "btn btn-primary menu-btn" })
                    }
                </div>
            </div>
        </div>

        <!-- ==================================== CONTENIDO CENTRAL ====================================== -->
        <div class="layout-center">
            <div class="layout-content">
                @RenderBody()
            </div>
        </div>

        <!-- ==================================== FOOTER ====================================== -->
        <div class="layout-south">
            @if (IsSectionDefined("footer"))
            {
                @RenderSection("footer")
            }
            else
            {
                <div class="text-muted small w-100 d-flex justify-content-between px-2">
                    <span>&copy; @DateTime.Now.Year - Monster University</span>
                    <span>Versión 1.0.0</span>
                </div>
            }
        </div>

    </div>

    <!-- Scripts -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

    @RenderSection("scripts", required: false)

</body>
</html>